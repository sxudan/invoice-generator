export type Theme = {
  primaryColor: string;
  secondaryColor: string;
};

const theme1: Theme = {
  primaryColor: "#555555",
  secondaryColor: "#fff",
};

const theme2: Theme = {
  primaryColor: "#e56e18",
  secondaryColor: "#fff",
};

const theme3: Theme = {
  primaryColor: "#1d4ab8",
  secondaryColor: "#fff",
};

const theme4: Theme = {
  primaryColor: "#2f9e85",
  secondaryColor: "#fff",
};

const theme5: Theme = {
  primaryColor: "#b71e1e",
  secondaryColor: "#fff",
};

export { theme1, theme2, theme3, theme4, theme5 };
